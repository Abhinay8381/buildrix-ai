apiVersion: apps/v1
kind: Deployment
metadata:
  name: buildrix-proxy
  namespace: buildrix-apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: buildrix-proxy
  template:
    metadata:
      labels:
        app: buildrix-proxy
    spec:
      containers:
        - name: proxy
          image: buildrix-proxy-server:latest
          imagePullPolicy: IfNotPresent
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
          env:
            - name: REDIS_URL
              # Refers to the service in the same namespace
              value: "redis://redis-service:6379"
          ports:
            - containerPort: 80

---

apiVersion: v1
kind: Service
metadata:
  name: buildrix-proxy-svc
  namespace: buildrix-apps
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: buildrix-proxy